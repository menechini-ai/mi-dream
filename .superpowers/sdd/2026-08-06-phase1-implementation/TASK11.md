# Task 11: Linter + Safety — Manual Steps Required

Ruff config already added to pyproject.toml in Task 1.

Run these commands after installing dependencies:

```bash
pip install ruff
ruff check src/ tests/
```

Fix any E/F/I/UP violations found.
